import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import { Stack } from 'expo-router'

//같은 폴더에 있는 파일들의 레이아웃을 정해줌(stack으로 정한다)
const Layout = () => {
  return (
    <Stack />
    //기본적으로 헤더가 생김
  )
}

export default Layout

const styles = StyleSheet.create({})